/**
 * Nombre: Permutaciones usando la biblioteca
 * Autor: rcc
 * Descripción: Generación de permutaciones usando la biblioteca.
 * Complejidad: $O(n!\cdot n)$
 */
#include <algorithm>

int n;
int arr[MAX]; // inicializar con { 0, 1, ..., n - 1 }
do {
   // procesar
} while (std::next_permutation(&arr[0], &arr[n]));
